{
  "name": "cryovault",
  "version": "1.0.0",
  "description": "Laboratory Cryogenic Storage Tank Inventory System — full-stack Node.js/SQLite app",

  "main": "server.js",

  "scripts": {
    "start":   "node server.js",
    "dev":     "npx nodemon server.js",
    "db:reset": "node -e \"require('fs').existsSync('./cryovault.db') && require('fs').unlinkSync('./cryovault.db'); console.log('DB deleted — will recreate on next start');\""
  },

  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors":           "^2.8.5",
    "dotenv":         "^16.4.5",
    "express":        "^4.18.2",
    "html5-qrcode":   "^2.3.8",
    "node-cron":      "^3.0.3",
    "qrcode":         "^1.5.3",
    "uuid":           "^9.0.0"
  },

  "devDependencies": {
    "nodemon": "^3.0.3"
  },

  "engines": {
    "node": ">=18.0.0"
  }
}
